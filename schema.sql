DROP TABLE if EXISTS treasures;

CREATE TABLE treasures (
	id INTEGER,
    name text,
	year text,
    img text,
    img2_type text,
	info text,
	location text,
	page_name text,
	category text
	
);

